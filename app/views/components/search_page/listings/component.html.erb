<section>
  <h2 class="text-2xl font-bold">Listings</h2>

  <div
    class="
      grid
      grid-cols-3
      gap-8
      mt-4
    "
  >
    <% listings.each do |listing| %>
      <%= component("ListingTile", listing: listing) %>
    <% end %>
  </div>
</section>